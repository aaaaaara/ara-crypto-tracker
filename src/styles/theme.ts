const size = {
	mobile: "600px",
	desktop: "1800px"
};

const theme = {
	mainColor: "#0a4297",
	mobile: `(max-width: ${size.mobile})`,
	desktop: `(min-width: ${size.desktop})`
};

export default theme;
